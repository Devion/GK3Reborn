<Project>

  <PropertyGroup>
    <!-- The checkout root, for the few things that live beside the solution rather than
         inside a project: the hand-dropped native payload in libs/<rid>, above all. -->
    <RebornRoot>$([MSBuild]::NormalizeDirectory('$(MSBuildThisFileDirectory)'))</RebornRoot>
  </PropertyGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- PlatformTarget alone forces 64-bit codegen. Declaring <Platforms>x64</Platforms>
         as well would add a platform dimension the solution has no configuration for,
         which leaves Visual Studio unable to map "Any CPU" onto any project - the
         projects then cannot be built or started from the IDE. -->
    <PlatformTarget>x64</PlatformTarget>
  </PropertyGroup>

  <!-- Correctness policy: see Plan/01-architecture.md section 2. -->
  <PropertyGroup>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <WarningLevel>9999</WarningLevel>
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
    <AnalysisLevel>latest-recommended</AnalysisLevel>
    <EnableNETAnalyzers>true</EnableNETAnalyzers>
    <Deterministic>true</Deterministic>
    <ContinuousIntegrationBuild Condition="'$(CI)' == 'true'">true</ContinuousIntegrationBuild>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <NoWarn>$(NoWarn);CS1591</NoWarn>
  </PropertyGroup>

  <PropertyGroup>
    <Product>GK3Reborn</Product>
    <Company>GK3Reborn contributors</Company>
    <Copyright>Copyright (C) 2026 GK3Reborn contributors</Copyright>
    <PackageLicenseExpression>GPL-3.0-or-later</PackageLicenseExpression>
    <VersionPrefix>0.1.0</VersionPrefix>
  </PropertyGroup>

</Project>
