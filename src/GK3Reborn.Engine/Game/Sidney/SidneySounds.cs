namespace GK3Reborn.Game.Sidney;

/// <summary>
/// The noises Sidney makes, by the names the archives hold them under.
/// </summary>
/// <remarks>
/// The original names eleven of them in its executable — SidEnter, SidExit, SidModem,
/// SidButton, SidButton2, SidButn-1, SidScan, SidScanner, SidPrintID,
/// SidAnagramScramble and NewEMail — and the port plays only the last. The rest are
/// the buttons and the machine itself, and nothing reads them yet.
/// </remarks>
public static class SidneySounds
{
    /// <summary>Post waiting, played on the way in.</summary>
    public const string NewMail = "NEWEMAIL.WAV";
}
